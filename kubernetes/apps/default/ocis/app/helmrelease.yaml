---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app ocis
spec:
  interval: 1h
  timeout: 15m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      ocis:
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            fsGroup: 1000
            fsGroupChangePolicy: OnRootMismatch
        initContainers:
          init-config:
            image:
              repository: docker.io/owncloud/ocis
              tag: 8.0.3@sha256:5fcf1ff578a8979c44696d9226c62743646f4917c87c883b432a40a5fb1c8c5b
            command:
              - /bin/sh
              - -c
            args:
              - |
                if [ ! -f /etc/ocis/ocis.yaml ]; then ocis init; else exit 0; fi
            env:
              OCIS_INSECURE: true
        containers:
          ocis:
            image:
              repository: docker.io/owncloud/ocis
              tag: 8.0.3@sha256:5fcf1ff578a8979c44696d9226c62743646f4917c87c883b432a40a5fb1c8c5b
            env:
              IDM_CREATE_DEMO_USERS: false
              OCIS_INSECURE: false
              OCIS_LOG_COLOR: true
              OCIS_LOG_LEVEL: warn
              OCIS_LOG_PRETTY: true
              OCIS_URL: https://arai.${PUBLIC_DOMAIN}
              OCIS_SHARING_PUBLIC_SHARE_MUST_HAVE_PASSWORD: true
              PROXY_DEBUG_ADDR: 0.0.0.0:9205
              PROXY_TLS: false
              TZ: ${TIMEZONE}
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
              limits:
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
    service:
      app:
        controller: *app
        ports:
          http:
            port: 9200
            primary: true
          metrics:
            port: 9205
    serviceMonitor:
      app:
        serviceName: *app
        endpoints:
          - port: metrics
    route:
      internal:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/name: oCIS
          gethomepage.dev/group: Productivity
          gethomepage.dev/icon: owncloud.png
        parentRefs:
          - name: internal
            namespace: networking
            sectionName: https
        hostnames: ["arai.${SECRET_DOMAIN}"]
        rules:
          - backendRefs:
              - identifier: app
                port: http
      public:
        parentRefs:
          - name: external
            namespace: networking
            sectionName: https
        hostnames: ["arai.${PUBLIC_DOMAIN}"]
        rules:
          - backendRefs:
              - identifier: app
                port: http
    persistence:
      config:
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        retain: true
        size: 1Gi
        storageClass: longhorn
        globalMounts:
          - path: /etc/ocis
            subPath: config
      web-config:
        type: configMap
        name: ocis
        globalMounts:
          - path: /etc/ocis/web.yaml
            subPath: web.yaml
            readOnly: true
      data:
        type: nfs
        server: "${NFS_SERVER}"
        path: "${NFS_KUBERNETES}/ocis"
        globalMounts:
          - path: /var/lib/ocis
            subPath: data
      tmpfs:
        type: emptyDir
        advancedMounts:
          ocis:
            app:
              - path: /tmp
                subPath: tmp
