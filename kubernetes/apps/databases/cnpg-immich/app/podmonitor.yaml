---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cnpg-immich
  namespace: databases
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: cnpg-immich
  podMetricsEndpoints:
    - port: metrics
